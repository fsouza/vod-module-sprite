dist: xenial
language: go
sudo: false
install:
  - curl https://install.goreleaser.com/github.com/golangci/golangci-lint.sh | BINDIR=$GOPATH/bin sh
  - go mod download
script:
  - golangci-lint run --enable-all -D lll -D errcheck
  - go test -race -coverprofile=coverage.txt -covermode=atomic -count 20
after_success: bash <(curl -s https://codecov.io/bash)
env:
  global:
    - GO111MODULE=on
    - GOPROXY=https://proxy.golang.org
go:
  - 1.11.x
  - 1.12.x
matrix:
  fast_finish: true
